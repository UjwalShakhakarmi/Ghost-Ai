# Ghost-Ai

This is a Next.js project built with Tailwind CSS v4, shadcn/ui primitives, and dark theme support.

## Getting Started

First, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
